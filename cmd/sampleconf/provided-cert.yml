proxyconfig:
  binds:
    - localhost:443
  reverseproxyurl: https://localhost
  redirecturls:
  - http://localhost
  tlsconfig:
    certificate:
      certfile: /var/cells/certs/cert.pem
      keyfile: /var/cells/certs/key.pem

