apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cells-chart.fullname" . }}-test-connection"
  labels:
    {{- include "cells-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cells-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
